# {{KTM_CREATE_PROJECT_NAME}}

Editable Kinematic Trees `python` project created from a KTM-installed kt-node boilerplate package.

This project is intentionally copied out of KTM's immutable package store. Edit this directory, not the installed boilerplate package.

## Dependency model

The generated `package.ktm.json` declares a dependency on `kinematic-trees/kt-node@{{KTM_CREATE_KT_NODE_VERSION}}`.
Build and test scripts expect KTM to provide dependency exports such as `CPATH`, `LIBRARY_PATH`, `LD_LIBRARY_PATH`, `PKG_CONFIG_PATH`, `CMAKE_PREFIX_PATH`, and `PATH`.

## Next commands

```bash
scripts/test.sh
ktm validate package.ktm.json
ktm pack --platform {{KTM_CREATE_PRIMARY_PLATFORM}}
```
